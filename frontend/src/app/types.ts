export interface Skill{
  _id: string;
  name: string;
  desc: string;
  frame: string;
  lang: string;
  img: string;
}

export interface Project{
  name: string;
  desc: string;
  img: string;
  link: string;
}